[build]
  builder = "dockerfile"
  dockerfile = "./Dockerfile"

[deploy]
  startCommand = "node packages/server/dist/src/index.js"
  healthcheckPath = "/health"
  healthcheckTimeout = 30
  restartPolicyType = "ON_FAILURE"
  restartPolicyMaxRetries = 3
